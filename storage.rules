rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Reglas para los archivos de juegos
    match /games/{gameId}/{allPaths=**} {
      // Permitir lectura pública
      allow read: if true;
      
      // Permitir escritura para usuarios autenticados
      allow write: if request.auth != null;
    }
  }
}
